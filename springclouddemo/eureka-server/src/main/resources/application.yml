server:
  port : 8761

eureka:
  instance:
    hostname : localhost
  client:
    registerWithEureka : false
    fetchRegistry : false
    serviceUrl:
#      ·şÎñ×¢²áµØÖ·
      defaultZone : http://${eureka.instance.hostname}:${server.port}/eureka/
